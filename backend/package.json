{
  "name": "velokarte-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "migrate": "bun run migrate.ts",
    "refresh-pmtiles": "bun run refresh-pmtiles.ts",
    "test": "bun test"
  },
  "dependencies": {
    "osmtogeojson": "^3.0.0-beta.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
